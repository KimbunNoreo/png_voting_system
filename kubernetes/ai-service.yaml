apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
spec:
  replicas: 0
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      containers:
        - name: ai-service
          image: securevote/ai-service:disabled
